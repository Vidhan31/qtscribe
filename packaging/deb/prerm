#!/bin/sh
set -e

case "$1" in
    remove|deconfigure)
        rm -f /usr/bin/qtscribe
        rm -f /usr/bin/keyinjectord
        ;;
    upgrade|failed-upgrade)
        ;;
    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 1
        ;;
esac

exit 0
